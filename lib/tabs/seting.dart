import 'package:flutter/material.dart';

class SetingTab extends StatelessWidget {
  const SetingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown,
    );
  }
}
